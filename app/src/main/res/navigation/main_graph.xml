<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_graph"
    app:startDestination="@id/videoListFragment">
    <fragment
        android:id="@+id/videoListFragment"
        android:name="com.example.videoplayer.VideoListFragment"
        android:label="VideoListFragment" >
        <action
            android:id="@+id/viewVideoPlayer"
            app:destination="@id/videoPlayerFragment" />
    </fragment>
    <fragment
        android:id="@+id/videoPlayerFragment"
        android:name="com.example.videoplayer.VideoPlayerFragment"
        android:label="VideoPlayerFragment" />
</navigation>